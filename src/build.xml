<!-- default is buildjar -->
<!-- use JAR instead of classes becaiuse it is easier to debug errors -->

<project name="webrod" default="buildjar" basedir=".">

  <!-- set global properties for this build -->
  <property name="src" value="."/>
  <property name="classes.dir" value="../WEB-INF/classes"/>

  <target name="compile">
    <!-- Compile the java code from ${src} into ${build} -->
    <javac debug="on" deprecation="off" srcdir="${src}" destdir="${classes.dir}"
		   classpath="../WEB-INF/lib/xmlserver.jar;../WEB-INF/lib/uit-client.jar;../WEB-INF/lib/servlet.jar;../WEB-INF/lib/log4j.jar;../WEB-INF/lib/uit-security.jar;../WEB-INF/lib/eionet-dir.jar"/>
  </target>

	<target name="buildjar" depends="compile"> 
    <jar basedir="${classes.dir}" jarfile="../WEB-INF/lib/rod.jar" includes="**/*.class"/>
		<delete dir="${classes.dir}/eionet"/>
	</target>

</project>
